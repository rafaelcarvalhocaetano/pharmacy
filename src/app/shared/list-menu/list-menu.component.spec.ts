import { ListMenuComponent } from './list-menu.component';

describe('ListMenuComponent', () => {
  let component: ListMenuComponent;

  beforeEach(() => {
    component = new ListMenuComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
