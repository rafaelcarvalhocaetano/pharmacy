import { ModalComponent } from './modal.component';

describe('ModalComponent', () => {

  let component: ModalComponent;

  beforeEach(() => component = new ModalComponent());

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
