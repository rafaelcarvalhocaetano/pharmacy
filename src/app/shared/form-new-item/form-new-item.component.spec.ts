import { FormNewItemComponent } from './form-new-item.component';

describe('FormNewItemComponent', () => {
  let component: FormNewItemComponent;

  beforeEach(() => {
    component = new FormNewItemComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
